const INITIAL_STATE = {
  loading: false,
  history: [],
  error: null
}

const historyReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default historyReducer
